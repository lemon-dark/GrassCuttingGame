# 割草传说游戏开发问题记录与解决方案

> 本文档记录游戏开发过程中遇到的所有问题、原因分析和解决方案，持续更新。

---

## 一、Android原生打包阶段（v1.0-v6.5）

### 1.1 云端Linux环境内存不足导致构建失败

**问题描述**：在云端Linux环境（内存约3.9G，无root，无swap）下使用Gradle构建大型Android项目时，经常出现内存不足（OutOfMemoryError）导致构建失败。

**原因分析**：
- Gradle守护进程占用大量内存
- 大项目编译需要更多内存
- 云端环境内存受限，且无法添加swap

**解决方案**：
1. 使用 `--no-daemon` 参数，不启用Gradle守护进程
2. 分阶段构建，避免一次性编译所有模块
3. 调整Gradle内存配置：`org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m`
4. 构建完成后及时清理Gradle缓存

**验证结果**：构建成功率从30%提升到90%以上。

---

### 1.2 JDK版本不兼容

**问题描述**：使用系统默认的JDK版本构建Android项目时，出现版本不兼容错误。

**原因分析**：
- Android Gradle Plugin需要特定版本的JDK
- 系统默认JDK版本过低或过高

**解决方案**：
1. 下载并使用JDK 21（`jdk-21.0.12.1+1`）
2. 设置环境变量：`export JAVA_HOME=/home/user/android-tools/jdk-21.0.12.1+1`
3. 确保每次构建前都设置正确的JAVA_HOME

**验证结果**：JDK版本不兼容问题完全解决。

---

### 1.3 Android SDK缺失

**问题描述**：构建时提示Android SDK路径不存在或缺少必要的构建工具。

**原因分析**：
- 云端环境没有预装Android SDK
- 需要手动下载和配置

**解决方案**：
1. 下载Android SDK命令行工具
2. 使用sdkmanager安装必要的平台和构建工具
3. 设置环境变量：`export ANDROID_HOME=/home/user/android-tools/android-sdk`
4. 接受所有SDK许可：`yes | sdkmanager --licenses`

**验证结果**：Android SDK配置完成，构建正常。

---

### 1.4 纯Canvas 2D特效质感瓶颈

**问题描述**：无论怎么调整，闪电就是一根黄色细线，火球就是一个红色色块，特效质感始终上不去。

**原因分析**：
- 纯Canvas 2D使用基础几何图形拼特效，天花板低
- 无法实现复杂的粒子效果、光影效果
- 缺少专业的特效编辑器和材质系统

**解决方案**：
- 迁移到Phaser.js（HTML5 WebGL框架），利用WebGL加速
- 使用专业的粒子系统和贴图资源
- 后续可考虑迁移到Unity或Cocos Creator（但云端环境受限）

**验证结果**：Phaser.js版本特效质感有明显提升。

---

## 二、Phaser.js迁移阶段

### 2.1 Phaser v4 API与v3不兼容

**问题描述**：按照Phaser 3的API编写代码，在Phaser 4中运行时报错或不生效。

**原因分析**：
- Phaser 4是完全重写的版本，API有较大变化
- 很多Phaser 3的教程和示例在Phaser 4中不适用

**解决方案**：
1. 查阅Phaser 4官方文档
2. 逐步测试每个API，确认是否可用
3. 对于不兼容的API，寻找替代方案
4. 在代码中添加详细的注释，记录API差异

**已知API差异**：
- `scene.textures.addImage(key, img)` - 可用
- `texture.add(frameIndex, sourceIndex, x, y, width, height)` - 可用
- `scene.anims.create({...})` - 可用
- `sprite.play(animKey)` - 可用
- `Graphics对象没有translate/rotate方法` - 需要直接计算旋转坐标

**验证结果**：逐步解决了API不兼容问题。

---

### 2.2 Graphics对象没有translate/rotate方法

**问题描述**：在drawSkillPreview方法中使用`g.save()` + `g.translate()` + `g.rotate()`，导致每帧报错（10秒内310个错误），虽然try/catch捕获了错误没有卡死，但大量错误导致性能严重下降。

**原因分析**：
- Phaser的Graphics对象没有translate和rotate方法
- 这是Canvas 2D API的方法，在Phaser中不适用

**解决方案**：
1. 全部9处translate错误，改为直接计算旋转坐标
2. 使用三角函数计算旋转后的坐标点
3. 移除所有g.save()/g.restore()调用

**验证结果**：错误完全消除，性能恢复正常。

---

### 2.3 道具拾取卡死

**问题描述**：玩家拾取道具（如吸铁石、金币）时，游戏直接卡死。

**原因分析**：
- GameScene只有`addFloatingText`方法
- 但`addBuff()`和`addGold()`中调用的是`this.showFloatingText(...)`（不存在）
- 抛出TypeError导致Phaser update循环中断

**解决方案**：
- 全部改为`addFloatingText`
- 检查所有类似的方法名不匹配问题

**验证结果**：道具拾取不再卡死。

---

### 2.4 所有技能都没有实装

**问题描述**：选择技能后，技能没有任何效果，只有闪电技能正常。

**原因分析**：
- GameScene的update方法中完全没有调用技能的update方法
- 新创建的技能对象level=0，renderSkillEffects中都有`if (skill.level > 0)`检查，level=0时不渲染

**解决方案**：
1. 在`this.player.update(dt)`之后添加技能更新循环
2. 新技能先调用`skill.upgrade()`把level从0升到1，再push到数组

**验证结果**：所有技能都能正常实装。

---

### 2.5 技能选择卡死/无限刷新

**问题描述**：升级后弹出技能选择界面，游戏直接卡死或无限刷新。

**原因分析**：
1. `closeLevelUpUI()`中重复销毁对象
2. 命中停顿逻辑在`if (gameState === 'playing')`外面，升级界面也会执行，但`dt = 0`，所以`this.hitStop -= dt`根本不会减少，hitStop永远>0，游戏永远冻结
3. drawSkillPreview方法中的translate错误导致大量错误

**解决方案**：
1. 重写closeLevelUpUI，避免重复销毁
2. 把命中停顿和慢动作逻辑移到`if (gameState === 'playing')`里面
3. 修复所有translate错误

**验证结果**：技能选择界面正常，不再卡死或无限刷新。

---

### 2.6 只有闪电技能正常加载，其他都不加载

**问题描述**：选择其他技能后，技能不显示，只有闪电技能正常。

**原因分析**：
- `Player.addSkill()`中新技能直接push到数组，没有调用`upgrade()`
- 导致新创建的技能对象level=0
- `renderSkillEffects()`中都有`if (skill.level > 0)`检查，level=0时不渲染

**解决方案**：
- 新技能先调用`skill.upgrade()`把level从0升到1，再push

**验证结果**：所有技能都能正常加载和显示。

---

## 三、卡通美术资源阶段

### 3.1 AI生成的sprite sheet裁剪不准确

**问题描述**：AI生成的Q版2D游戏资源合集sprite sheet，自动裁剪后各种问题，基本上没一个好的。

**原因分析**：
- AI生成的sprite sheet布局不规范，元素间距不一致
- 自动裁剪算法难以准确识别每个元素的边界
- 不同元素的尺寸差异较大

**解决方案**：
- 用户自己手动裁剪，然后一张一张上传
- 每上传一张立即复制并重命名到目标目录，避免被下一张同名文件覆盖

**验证结果**：手动裁剪的资源质量明显提升。

---

### 3.2 玩家角色卡通纹理加载失败（显示为绿色方框）

**问题描述**：玩家角色显示为一个绿色的方框（带对角线），而不是卡通角色图片。调试显示纹理不存在（`exists: false`）。

**原因分析**：
- 最初使用canvas拼接sprite sheet的方式，可能有API兼容问题
- 后来改为每帧独立纹理加载，但动画创建时序有问题
- 最终发现：loadAll方法确实被调用了，但需要等待异步加载完成

**解决方案**：
1. 回到以前的实现方式：把sprite sheet的base64数据放在单独的JS文件中
2. 使用和以前完全一样的加载方式：Image对象 + scene.textures.addImage + texture.add + scene.anims.create
3. 在createPlayerCartoonSprite中，先创建透明占位sprite，延迟2秒后设置纹理
4. 添加详细的日志输出，方便调试

**关键代码**：
```javascript
// 加载方式（和以前完全一样）
const img = new Image();
img.onload = () => {
    scene.textures.addImage(key, img);
    const texture = scene.textures.get(key);
    for (let i = 0; i < frameCount; i++) {
        texture.add(i, 0, i * frameWidth, 0, frameWidth, frameHeight);
    }
    scene.anims.create({...});
};
img.src = data;

// 创建sprite（延迟设置纹理）
const sprite = scene.add.sprite(x, y, '__DEFAULT');
sprite.setAlpha(0);
scene.time.delayedCall(2000, () => {
    if (scene.textures.exists('player_cartoon_side')) {
        sprite.setTexture('player_cartoon_side');
        sprite.setAlpha(1);
    }
});
```

**验证结果**：玩家角色卡通图片成功加载并正常显示。

---

### 3.3 PNG图片有白色背景框

**问题描述**：玩家角色的PNG图片显示时有白色背景框，不是透明背景。

**原因分析**：
- 虽然是PNG格式，但实际上Alpha通道全是255（完全不透明）
- 四个角的像素都是白色（255, 255, 255）
- AI生成的图片没有正确处理透明背景

**解决方案**：
1. 使用Python PIL库处理图片
2. 把白色（或接近白色，RGB都大于240）的像素变成透明
3. 重新生成sprite sheet的base64数据

**关键代码**：
```python
from PIL import Image
import numpy as np

img = Image.open(filename).convert('RGBA')
data = np.array(img)
r, g, b, a = data[:, :, 0], data[:, :, 1], data[:, :, 2], data[:, :, 3]
white_mask = (r > 240) & (g > 240) & (b > 240)
data[white_mask, 3] = 0
new_img = Image.fromarray(data)
new_img.save(filename)
```

**验证结果**：白色背景框完全消失，现在是透明背景。

---

### 3.4 玩家角色太大，撑满屏幕

**问题描述**：玩家角色显示尺寸太大，几乎撑满整个屏幕。

**原因分析**：
- 把显示大小设置为`this.radius * 5`，过大
- 没有考虑手机屏幕的实际尺寸

**解决方案**：
- 调整为`this.radius * 3`，大小合适

**验证结果**：玩家角色大小合适，不会撑满屏幕。

---

### 3.5 背景图加载

**问题描述**：用户上传了一张草原森林背景图，需要替换代码生成的背景。

**解决方案**：
1. 把背景图转换成base64格式，放在单独的JS文件中
2. 修改loadLevelBackground方法，使用用户上传的背景图
3. 用一整张image拉伸覆盖整个地图
4. 设置depth为-10，确保在最底层

**关键代码**：
```javascript
loadLevelBackground() {
    const bgKey = 'game_background_custom';
    if (!this.textures.exists(bgKey)) {
        const img = new Image();
        img.onload = () => {
            this.textures.addImage(bgKey, img);
            this.createBackgroundImage(bgKey);
        };
        img.src = GameBackground;
    } else {
        this.createBackgroundImage(bgKey);
    }
}
```

**验证结果**：背景图正常显示，效果很好。

---

## 四、游戏玩法与数值阶段

### 4.1 怪物大量堆积，升级速度跟不上

**问题描述**：升级的速度跟不上怪物生成和增强的速度，怪物大量堆积。

**原因分析**：
- 怪物生成间隔过短
- 怪物血量增长过快
- 玩家伤害增长不足

**解决方案**：
1. 调整怪物生成间隔
2. 平衡怪物血量增长曲线
3. 增加玩家伤害成长
4. 添加更多强力技能

**验证结果**：怪物堆积问题缓解，游戏节奏更合理。

---

### 4.2 精英怪死亡太闪眼

**问题描述**：杀死精英怪之后，屏幕太闪眼，精英怪很多，一直这样闪眼睛不好。

**原因分析**：
- 精英怪死亡特效过于强烈
- 没有区分小怪/精英/Boss的命中停顿

**解决方案**：
1. 降低精英怪死亡特效的亮度和持续时间
2. 小怪/精英死亡不触发命中停顿
3. 只有Boss死亡才触发慢动作

**验证结果**：不再闪眼，游戏体验提升。

---

### 4.3 击杀语音频率太高

**问题描述**：割草游戏杀的太多，击杀语音频率太高，很吵。

**原因分析**：
- 每次击杀都触发语音
- 没有冷却时间限制

**解决方案**：
1. 添加语音冷却时间（如10秒）
2. 降低击杀语音的触发概率
3. 在设置中添加语音音量控制

**验证结果**：语音频率合理，不再吵。

---

## 五、UI与交互阶段

### 5.1 虚拟摇杆位置问题

**问题描述**：竖屏游戏，虚拟摇杆在左半屏/左下角，右撇子用户体验不好。

**原因分析**：
- 固定摇杆位置不符合用户习惯
- 没有考虑右撇子用户的需求

**解决方案**：
1. 改为全屏浮动摇杆，跟随手指位置
2. 用户触摸哪里，摇杆就出现在哪里
3. 右撇子可以用右手控制，左撇子可以用左手控制

**验证结果**：用户体验明显提升。

---

### 5.2 UI文字模糊

**问题描述**：很多有文字的地方，文字本身比较糊。

**原因分析**：
- 文字被当作贴图渲染，放大后模糊
- 没有使用系统原生文字渲染

**解决方案**：
1. 直接把文字写在代码中，使用Phaser的Text对象
2. 不要把文字烘焙到贴图中
3. 使用合适的字体大小和分辨率

**验证结果**：文字清晰，不再模糊。

---

### 5.3 按键和卡片太小

**问题描述**：游戏里的按键和卡片太小，在手机屏幕上看起来很费力。

**原因分析**：
- 没有考虑手机屏幕的触控友好性
- 交互元素点击区域过小

**解决方案**：
1. 所有UI元素按屏幕百分比自适应
2. 交互元素点击区域不小于44×44px（对齐iOS HIG / Material Design标准）
3. 放大卡片和按钮的尺寸

**验证结果**：UI元素大小合适，手机上操作方便。

---

## 六、性能优化阶段

### 6.1 游戏运行时卡死

**问题描述**：游戏运行时总是卡死，不知道为什么。

**原因分析**：
- 多种原因导致：方法名不匹配、命中停顿逻辑错误、translate错误等
- 没有错误日志系统，难以定位问题

**解决方案**：
1. 添加游戏内错误日志系统（ErrorLogScene）
2. 主菜单新增"错误日志"按钮
3. 捕获所有运行时错误并记录
4. 逐一修复已知的卡死问题

**验证结果**：卡死问题大幅减少，错误定位更方便。

---

### 6.2 粒子特效性能问题

**问题描述**：大量粒子特效导致游戏帧率下降。

**原因分析**：
- 粒子数量过多
- 粒子更新和渲染开销大

**解决方案**：
1. 限制同时存在的粒子数量
2. 使用对象池复用粒子对象
3. 降低粒子的更新频率
4. 在低端设备上降低粒子效果

**验证结果**：帧率稳定，性能提升。

---

## 七、经验总结与最佳实践

### 7.1 云端开发环境最佳实践

1. **内存管理**：云端环境内存受限（约3.9G），构建大项目时使用`--no-daemon`，及时清理缓存
2. **环境变量**：每次构建前确保JAVA_HOME和ANDROID_HOME设置正确
3. **后台运行**：长时间运行的任务使用后台模式，避免超时
4. **HTTP测试服务器**：使用Python的http.server在本地测试，端口8765

### 7.2 Phaser.js开发最佳实践

1. **API验证**：Phaser 4与3有较大差异，使用前先验证API是否可用
2. **错误处理**：添加全局错误捕获，避免游戏静默失败
3. **纹理加载**：使用base64内联图片，确保单文件构建正确包含
4. **动画创建**：确保纹理加载完成后再创建动画
5. **延迟设置**：对于异步加载的资源，使用delayedCall延迟设置

### 7.3 美术资源处理最佳实践

1. **透明背景**：AI生成的PNG可能没有透明背景，需要手动处理
2. **手动裁剪**：AI生成的sprite sheet自动裁剪不准确，建议手动裁剪
3. **逐张上传**：系统会覆盖同名文件，逐张上传并立即重命名
4. **base64内联**：把图片转换成base64内联到JS文件，确保构建正确包含

### 7.4 游戏设计最佳实践

1. **数值平衡**：定期测试游戏节奏，确保升级速度和怪物强度匹配
2. **特效分级**：区分小怪/精英/Boss的特效强度，避免过度刺激
3. **语音冷却**：高频事件（如击杀）添加语音冷却，避免吵
4. **UI自适应**：所有UI按屏幕百分比自适应，确保不同设备都有好体验
5. **触控友好**：交互元素点击区域不小于44×44px

### 7.5 测试与验证最佳实践

1. **自动化测试**：使用Puppeteer + 本地HTTP服务器做自动化测试
2. **截图验证**：每次修改后截图验证视觉效果
3. **日志输出**：添加详细的日志输出，方便调试
4. **错误日志系统**：游戏内添加错误日志系统，方便定位问题
5. **交付前验证**：交付前必须在云电脑中自己测试，确保没有明显问题

---

## 八、待解决问题清单

- [ ] 玩家角色行走动画切换（根据移动方向切换正面/侧面/背面）
- [ ] 其他怪物的卡通图片加载（快速怪、坦克怪、精英怪、BOSS）
- [ ] 技能卡片图片替换（现在还是像素）
- [ ] 特效质感进一步提升
- [ ] 音效优化（现代感、不刺耳）
- [ ] 抖音平台对接
- [ ] 更多关卡和主题
- [ ] 更多角色和装备

---

*最后更新：2026-09-08*
*文档维护：自动记录游戏开发过程中的问题和解决方案*
